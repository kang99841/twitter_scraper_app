// Tells VS Code how to build and enter your dev container	
// Use when you want to code inside container with VS Code

  // Tells VS Code how to build and enter the container.
  // Auto-installs extensions and Python requirements.
  // Forwards ports for you (e.g., Flask server on port 5000).
  // Mounts your project folder into /app.
// Use this when you want a consistent dev environment across team members.


{
  "name": "Twitter Scraper API",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/app",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },


  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance",
    "ms-azuretools.vscode-docker"
  ],

  "remoteUser": "root",
  "runArgs": ["--cap-add=SYS_ADMIN", "--shm-size=1gb"],

}

